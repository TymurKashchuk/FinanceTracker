<UserControl x:Class="FinanceTracker.wpf.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FinanceTracker.wpf.ViewModels"
             xmlns:views="clr-namespace:FinanceTracker.wpf.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MainViewModel}"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- DataGrid з транзакціями -->
        <Border Background="White" 
                CornerRadius="8" 
                Padding="10" 
                Grid.Row="0" 
                Margin="0,0,10,0">
            <DataGrid x:Name="TransactionsDataGrid"
                      ItemsSource="{Binding Transactions}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="100"/>
                    <DataGridCheckBoxColumn Header="Income" Binding="{Binding IsIncome}" Width="80"/>
                    <DataGridTextColumn Header="Account" Binding="{Binding Account.Name}" Width="120"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="120"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="150"/>
                </DataGrid.Columns>

                <!-- Контекстне меню -->
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Delete" Click="DeleteTransaction_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Border>

        <!-- Підсумкові в’юхи -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <views:AccountSummaryView Grid.Row="0"/>
            <views:CategorySummaryView Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>